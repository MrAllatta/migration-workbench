active_epic_id: e06
active_story_id: e06s01

# Session harness — use this file to set active epic and story.
# The orchestrate.js script reads active_epic_id from here.
# The agent reads active.story_id from specs/execution-status.yaml.
#
# Previously this file held bigpowers build_epic state. That flow is
# retired — the session harness replaces it.
